name: Security Audit
on:
  schedule:
    - cron: '0 8 * * 1'  # Weekly on Mondays at 8 AM
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  secrets:
    name: Secret Scanning
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  sast:
    name: Subprocess & Code Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Bandit
        run: pip install bandit
      - name: Run Bandit
        # Focus strictly on high-severity issues and subprocess/os calls
        run: bandit -r src/ -ll -ii -f json -o bandit-report.json || true
      - name: Upload Report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report
          path: bandit-report.json

  deps:
    name: Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pip-audit
        run: pip install pip-audit
      - name: Run pip-audit
        # Ignore CVE-2026-25990 (Pillow) due to upstream moviepy constraint <12.0
        run: pip-audit -r requirements.txt --ignore-vuln CVE-2026-25990
