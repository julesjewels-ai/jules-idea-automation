1. **Update `pyproject.toml`**
   - Add a `[tool.coverage.run]` and `[tool.coverage.report]` section or update the `[tool.pytest.ini_options]` block to enforce `pytest-cov` minimum coverage.
   - We need to configure `pytest` to always run `pytest-cov` with `--cov=src/ --cov-report=xml --cov-report=term-missing --cov-fail-under=70`.
   - Also, ensure that `pytest-cov` is added explicitly to `[project.optional-dependencies] dev` according to memory instructions: "Development dependencies (like pytest, pytest-cov, mypy, ruff) must be explicitly declared in the [project.optional-dependencies] dev section of pyproject.toml."

2. **Update `.github/workflows/ci.yml`**
   - In the "Run tests" step, modify it to ensure `pytest` output uses `pytest-cov` (if it's not already covered by `addopts` in `pyproject.toml`, but it's best to rely on `addopts`).
   - Add a step to upload the `coverage.xml` generated by `pytest-cov` as a CI artifact.

3. **Update `.gitignore`**
   - Ensure `coverage.xml` is ignored since it says: "Test coverage XML reports (coverage.xml) are uploaded as CI artifacts and must be excluded in .gitignore."

4. **Verify changes**
   - Run `pytest` locally to ensure the coverage checks work.
   - Review file changes using `git diff`.

5. **Pre-commit step**
   - Run `pre_commit_instructions` and follow the checks.

6. **Submit PR**
   - Stage changes, use a conventional commit (`feat(ci): enforce 70% test coverage minimum`), and create a new branch. Submit using `submit`.
