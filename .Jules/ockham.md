## 2025-05-26 - [Dead Code Removal] **Observation:** Found `tool.py` referencing non-existent modules (`src.jules_client`) and duplicate/transitional top-level service files (`src/gemini_client.py`, etc.) that were superseded by `src/services/`. This created confusion and potential security risks (`src/scraper.py` lacked SSRF protection). **Action:** Deleted `tool.py`, `src/gemini_client.py`, `src/github_client.py`, and `src/scraper.py`. Verified no usages in active codebase via grep and tests.

## 2025-05-26 - [DRY Refactor] **Observation:** `GeminiClient` in `src/services/gemini.py` had duplicated logic for calling `generate_content`, configuring the model, and parsing JSON in three different methods (`generate_idea`, `extract_idea_from_text`, `generate_project_scaffold`). This increased maintenance burden and risk of inconsistency. **Action:** Extracted `_generate_content` helper method to encapsulate API calls, configuration, and error handling. Verified functionality with new unit tests in `tests/services/test_gemini.py`.
